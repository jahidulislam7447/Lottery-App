# iOS runner

This directory is reserved for the generated Flutter iOS runner. Run `flutter create .` from the project root on a machine with Flutter and Xcode installed to generate the complete Xcode workspace and Podfile for this app.
